# 设计说明

## 产品定义

帮妈妈问 AI 是一个面向中老年用户的单页应用。目标不是做复杂工作流，而是把“我不会用这个电子产品”这个瞬间压缩成一个最小闭环：

1. 妈妈把问题说出来，或拍一张图、传一段视频。
2. AI 根据当前长上下文继续回答，不拆分新会话。
3. 回复默认用文字 + 引导图 + 女声朗读给出。
4. 如果 AI 判断问题边界不清、存在风险、或继续解释成本很高，就提示联系女儿；再进一步，可以转给志愿者。

## 设计原则

- 单页：只有一个主场景，不切换多对话。
- 豆包风格：轻盈、圆角、悬浮卡片、干净浅色背景、内容优先。
- 适老化：按钮大、层级少、主操作清楚、避免专业术语。
- 连续感：会话历史持续保留，让妈妈像“继续说下去”一样使用。
- 低阻力：语音、拍照、相册、视频都放进一个发送入口附近。

## 信息架构

### 顶部

- 产品标题
- 会话编号
- 朗读开关
- 分享按钮

### 中间

- 聊天气泡流
- 用户消息显示文字和附件
- AI 消息显示文字、引导图、重播按钮、转发按钮
- 如需升级，出现“联系女儿/志愿者”提示卡

### 底部

- 多行输入框
- 大号语音按钮
- 图片上传按钮
- 视频上传按钮
- 发送按钮

## 技术设计

### 前端

- Vite + React + TypeScript
- 单入口 `App.tsx`
- 浏览器 `SpeechRecognition` 做语音输入
- 浏览器 `speechSynthesis` 做默认女声播报
- `FormData` 上传文本和附件

### 服务端

- Express + TypeScript
- `multer` 内存接收上传文件
- 内存 `Map` 保存单一会话和长上下文
- Azure OpenAI 处理文字 + 图片
- 视频只转成附件说明文字并带入上下文
- 服务端生成 SVG data URL 作为“AI 引导图”

## 会话模型

- 只有一个 session
- 前端将 `sessionId` 存到 localStorage
- 每次提问都把新消息追加到同一会话
- 服务端只保留最近 80 条消息，避免上下文无限膨胀
- 会话 24 小时不活跃自动过期

## 升级策略

### AI 继续处理

适用于：
- 页面结构清楚
- 用户目标明确
- 不涉及资金/身份风险

### 建议联系女儿

适用于：
- 用户连续表示“还是不懂/没找到/不会”
- 没有足够上下文判断下一步
- 图片不清晰但仍可继续人工接手

### 建议志愿者

适用于：
- 资金、身份、验证码、银行卡、医保等高风险信息
- AI 明显不适合继续给直接操作建议

## 这次重写的取舍

- 不做女儿端，先把妈妈端体验打通
- 不做视频逐帧分析，先支持视频上传和上下文接收
- 不接复杂数据库，先用内存存储把交互验证完
- 不做多路部署，先保证本地开发和 App Service 单服务可落地
